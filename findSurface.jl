function findSurface(alpha,beta,M,a,b,k,l;camera::NTuple{2,Float64}=(-30, 30), plotSlice::Bool=false, saveFig::Bool=false)

    const a_prime = a*cosd(alpha)
    const k_prime = k*cosd(beta)

    
end

function